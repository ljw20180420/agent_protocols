[project]
name = "agent-protocols"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ljw20180420", email = "ljw2017@sjtu.edu.cn" }
]
requires-python = ">=3.13"
dependencies = [
    "agent-client-protocol>=0.10.0",
    "pyyaml>=6.0.3",
]

[project.scripts]
agent-protocols = "agent_protocols:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
